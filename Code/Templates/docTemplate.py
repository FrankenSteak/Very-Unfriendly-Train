class yeet:

    def docExample(self, i: int, f: float, **kwargs) -> float:
        """
            Description:
            
                Trains the passed surrogate using thread techniques if required to do so. If 
                an optimizer is specified, only that optimizer will be used. However, if no 
                optimizer is specified a random optimizer will be used. 

            |\n
            |\n
            |\n
            |\n
            |\n

            Params:

                :param i: = ( int ) -- does a thing
                :param f: = ( float ) -- more thing

            |\n

            Args:

                + surrogate   = ( vars ) A surrogate class instance
                    ~ Required
                + password    = ( int ) Surrogate password
                    ~ Required
                + optimizer   = ( enum ) The enum of the optimizer to be used
                    ~ Default = { PSO }
                + threading   = ( bool ) Multi-treading flag
                    ~ Default = { False }

            |\n

            Returns:

                + surrogate   = ( vars ) The trained surrogate
        """

        #   STEP 0: Local variables
        #   STEP 1: Setup - local variables

        #   STEP 2: 
        self.docExample(0, 0.1, )

        #   STEP ??: Return
        return 0.0

